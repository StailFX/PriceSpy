# Secret key for JWT signing and session security
# Generate a unique, unpredictable value for production
tmp_SECRET_KEY=your-secret-key

# Redis connection URL for task queue and worker
# e.g. redis://localhost:6379 or redis://password@host:port/0
REDIS_URL=redis://redis:6379

# Database connection URL
# Default uses SQLite local file. For production, switch to Postgres, MySQL, etc.
DATABASE_URL=sqlite:///./db.sqlite3

# Uvicorn server settings
# Host and port for the FastAPI application
HOST=0.0.0.0
PORT=8000

# JWT configuration
# HS256 algorithm for signing tokens
ALGORITHM=HS256
# Token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30
